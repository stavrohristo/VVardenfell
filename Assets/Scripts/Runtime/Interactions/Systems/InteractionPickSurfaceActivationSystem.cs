namespace VVardenfell.Runtime.Interactions
{
    // Pick collider activation is handled by spawn, streaming, and ref-state transitions.
    // Keep this file present because Unity-generated project files may still reference it.
    static class InteractionPickSurfaceActivationSystem
    {
    }
}
